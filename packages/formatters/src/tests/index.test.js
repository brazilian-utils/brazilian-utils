import { formatCpf, formatCnpj } from '../index';

describe('formatCpf', () => {
  test('should have been imported correctly', () => {
    expect(formatCpf).not.toBeUndefined();
  });
});

describe('formatCnpj', () => {
  test('should have been imported correctly', () => {
    expect(formatCnpj).not.toBeUndefined();
  });
});
