export const LENGTH = 12;

/**
 * Total digit count for voter ids issued by São Paulo (01) and Minas Gerais (02), which may
 * carry a 9-digit sequential number instead of the usual 8-digit one.
 */
export const EXTENDED_LENGTH = 13;
